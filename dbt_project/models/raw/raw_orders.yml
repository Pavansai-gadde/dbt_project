version: 2

models:

  - name: raw_orders 
    config:
      tags: ['orders']
    tests:
      - dq_checks_test:
          arguments:
            model_name: raw_orders
            layer_name: raw   
          config:
            tags: ['dq_checks']  

    columns:
      - name: 'order_id'
        tags: ['postload','data_quality']
        data_tests:
          - unique
          - not_null    

           